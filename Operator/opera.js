// what is operator ?
// an operator perform some operation on single or multiple operand  .

// how many kind of operator in js ?
// In js 5 type  of operator .
// 1. arithmetic operators
// 2. logical operators .
// 3. comparison operators .
// 4. assignments operators .
// 5. conditional operators .


// Arithmetic operators are used to perform mathematical operations between numeric operands(+, - , * , / , %).

// comparison operator will compare oparends(==, >=, <= , !=, !==, > , <)

// bwirbise operator  (& , |, ^, >>, <<)
var a = 4
var b = 6
console.log(b ^ a)

// logical operator (&&, ||, !) (extra different && or & => & it will return binary value and && it will return boolean value )

// assignment operator (=, +=, -=, *=, %=, /=)

// conditional operator (ternay operator)
var age = 18
var status = (age >= 18) ? 'adult' : 'minor';
console.log(status)









